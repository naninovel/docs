import { ResolvedAsset, CapturedAsset } from "../asset";

/** Resolves type, content locations and specs of the captured syntax. */
export async function resolve(assets: CapturedAsset[]): Promise<ResolvedAsset[]> {
    return [];
}
